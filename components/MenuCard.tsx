'use client';
import {motion} from 'framer-motion';
import {ArrowUpRight,Plus} from 'lucide-react';
import {Product} from '@/types';
export default function MenuCard({product,onCustomize,index=0}:{product:Product;onCustomize:(p:Product)=>void;index?:number}){return <motion.article initial={{opacity:0,y:16}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.12}} transition={{delay:index*.04}} whileHover={{y:-6}} className="group overflow-hidden rounded-[1.7rem] border border-espresso/10 bg-white shadow-[0_18px_50px_rgba(30,20,15,.09)]"><div className="relative aspect-[1.05] overflow-hidden"><img src={product.image} alt={product.name} className="h-full w-full object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-black/55 via-transparent to-transparent"/><span className="absolute left-3 top-3 rounded-full bg-espresso/80 px-3 py-1.5 text-[9px] font-black uppercase tracking-widest text-oat backdrop-blur">{product.category}</span><span className="absolute bottom-3 right-3 grid h-10 w-10 place-items-center rounded-full bg-oat text-espresso transition group-hover:rotate-45"><ArrowUpRight size={17}/></span></div><div className="p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="display text-2xl font-black tracking-[-.03em]">{product.name}</h3><p className="mt-1 text-sm leading-6 text-espresso/50">{product.description}</p></div><strong className="whitespace-nowrap text-lg font-black text-terracotta">₹{product.price}</strong></div><button onClick={()=>onCustomize(product)} disabled={!product.available} className="btn mt-5 flex w-full items-center justify-center gap-2 rounded-2xl bg-espresso py-3.5 text-sm font-black text-oat disabled:cursor-not-allowed disabled:opacity-35">{product.available?<><Plus size={16}/>Customize & add</>:'Sold out'}</button></div></motion.article>}
